# ─────────────────────────────────────────────────────────────────────────────
# vibrationofawesome.com — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.
# ─────────────────────────────────────────────────────────────────────────────

# ── Anthropic (required for post generation + captions) ───────────────────────
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ── NASA APOD (astronomy images - free key at api.nasa.gov, instant via email) ─────
# Without a key DEMO_KEY is used automatically (30 req/hr, 50/day - fine for personal use)
NASA_API_KEY=your_nasa_api_key_here

# ── Dashboard password ────────────────────────────────────────────────────────
# Used to protect /dashboard/. Run syndicate.js once after setting this and
# it will auto-generate static/_data/dashboard-config.json with the SHA-256 hash.
DASHBOARD_PASSWORD=your_secure_dashboard_password

# ── Bluesky ───────────────────────────────────────────────────────────────────
# Handle: your full handle e.g. vibrationofawesome.bsky.social
# App Password: generate one at https://bsky.app/settings/app-passwords
BLUESKY_HANDLE=yourhandle.bsky.social
BLUESKY_APP_PASSWORD=your-app-password-here

# ── Mastodon ──────────────────────────────────────────────────────────────────
# Instance: full URL e.g. https://mastodon.social
# Access Token: generate at Settings > Development > New Application
MASTODON_INSTANCE=https://mastodon.social
MASTODON_ACCESS_TOKEN=your_mastodon_access_token

# ── Facebook / Meta ───────────────────────────────────────────────────────────
# App credentials (needed for long-lived token exchange — optional but recommended)
META_APP_ID=your_meta_app_id
META_APP_SECRET=your_meta_app_secret

# VOA Facebook Page
META_PAGE_ID_VOA=your_voa_facebook_page_id
META_PAGE_TOKEN_VOA=your_voa_page_access_token

# EarthStar Facebook Page
META_PAGE_ID_EARTHSTAR=your_earthstar_facebook_page_id
META_PAGE_TOKEN_EARTHSTAR=your_earthstar_page_access_token

# ── Pinterest ─────────────────────────────────────────────────────────────────
# OAuth 2.0 — requires boards:write and pins:write scopes.
# Run `node scripts/get-pinterest-token.js` to complete the OAuth flow.
# First add http://localhost:9877/callback as a redirect URI in your Pinterest app:
#   https://developers.pinterest.com/apps/ → your app → Edit → Redirect URIs
PINTEREST_APP_ID=your_pinterest_app_id
PINTEREST_APP_SECRET=your_pinterest_app_secret
PINTEREST_ACCESS_TOKEN=your_pinterest_oauth_access_token
PINTEREST_REFRESH_TOKEN=your_pinterest_refresh_token_if_provided
PINTEREST_BOARD_ID=your_target_board_id

# ── Dev.to ────────────────────────────────────────────────────────────────────
# Generate at https://dev.to/settings/extensions (API Keys section)
DEVTO_API_KEY=your_devto_api_key

# ── GitHub tokens (for syndication + feeder) ─────────────────────────────────
# GITHUB_TOKEN can be your primary PAT for repo automation.
# FEEDER_TRIGGER_TOKEN is preferred for cross-repo writes to Lordshrrred/TWTF_Feeder.
# Required scope/permission: contents:write on the feeder repo.
GITHUB_TOKEN=your_github_token
FEEDER_TRIGGER_TOKEN=your_feeder_repo_write_token

# ── Hashnode ──────────────────────────────────────────────────────────────────
# API key: https://hashnode.com/settings/developer
# Publication ID: found in your Hashnode publication dashboard > Settings > Advanced
# Blog slug: your-blog.hashnode.dev or custom domain slug
HASHNODE_API_KEY=your_hashnode_api_key
HASHNODE_PUBLICATION_ID=your_hashnode_publication_id
HASHNODE_BLOG_SLUG=your-blog

# ── Tumblr ────────────────────────────────────────────────────────────────────
# Create app at https://www.tumblr.com/oauth/apps
# Blog name: your-blog (without .tumblr.com)
TUMBLR_CONSUMER_KEY=your_tumblr_consumer_key
TUMBLR_CONSUMER_SECRET=your_tumblr_consumer_secret
TUMBLR_TOKEN=your_tumblr_oauth_token
TUMBLR_TOKEN_SECRET=your_tumblr_oauth_token_secret
TUMBLR_BLOG_NAME=your-tumblr-blog-name

# ── WordPress.com Direct API (optional, for pages/categories/media/site cleanup) ──
# Register an app in the WordPress.com Applications Manager and set a redirect URI.
# Docs: https://developer.wordpress.com/docs/api/oauth2/
# For your own site, you can mint a dev token with:
#   python3 scripts/get_wordpress_oauth_token.py --flow password --write-env
# Or generate an authorization URL for the full OAuth code flow with:
#   python3 scripts/get_wordpress_oauth_token.py --flow auth-url
WORDPRESS_CLIENT_ID=your_wordpress_com_app_client_id
WORDPRESS_CLIENT_SECRET=your_wordpress_com_app_client_secret
WORDPRESS_REDIRECT_URI=http://localhost:9878/callback
WORDPRESS_SCOPE=posts media
WORDPRESS_BLOG=https://your-site.wordpress.com
WORDPRESS_USERNAME=your_wordpress_com_username
WORDPRESS_APPLICATION_PASSWORD=your_wordpress_com_application_password
WORDPRESS_OAUTH2_TOKEN=your_wordpress_com_oauth2_access_token

# ── Instagram + Threads (via Publer) ─────────────────────────────────────────
# Publer handles Instagram and Threads posting via its scheduling API.
# API key: https://app.publer.com/settings/api
# Workspace ID + Account IDs: run `node -e "..." ` probe or check Publer dashboard.
#   → Workspace: Settings > Workspace > ID shown in URL or API response
#   → Account IDs: GET https://app.publer.com/api/v1/accounts (Bearer-API key)
PUBLER_API_KEY=your_publer_api_key
PUBLER_WORKSPACE_ID=your_publer_workspace_id
PUBLER_INSTAGRAM_ACCOUNT_ID=your_publer_instagram_account_id
PUBLER_THREADS_ACCOUNT_ID=your_publer_threads_account_id

# ── Facebook (user token) ─────────────────────────────────────────────────────
# Run `node scripts/get-facebook-token.js` to generate a long-lived user token
# and individual page tokens. Page tokens from long-lived user tokens never expire.
# META_USER_TOKEN refreshed via that script (valid ~60 days).
META_USER_TOKEN=your_meta_user_long_lived_token
